
import { ProspectSource, ProspectInterest, ProspectStage, TaskType, TaskStatus, Role, Branch } from './types';

export const PROSPECT_SOURCES = Object.values(ProspectSource);
export const PROSPECT_INTERESTS = Object.values(ProspectInterest);
export const PROSPECT_STAGES = Object.values(ProspectStage);
export const TASK_TYPES = Object.values(TaskType);
export const TASK_STATUSES = Object.values(TaskStatus);
export const USER_ROLES = Object.values(Role);
export const BRANCHES = Object.values(Branch);